import {timeline,judges,faq,REGISTRATION_URL} from "./data.js";
export function initContent(){
 const tl=document.getElementById("tl-items"); timeline.forEach(item=>{const d=document.createElement("div");d.className="tl-item";d.setAttribute("role","listitem");d.innerHTML=`<div class="tl-dot" aria-hidden="true"></div><div class="tl-card"><div class="tl-time">${item.time}</div><div class="tl-event">${item.event}</div><div class="tl-phase">${item.phase}</div></div>`;tl?.appendChild(d);});
 const grid=document.getElementById("judges-grid"); judges.forEach(j=>{const d=document.createElement("article");d.className="judge-card reveal";d.innerHTML=`<div class="judge-avatar" aria-hidden="true">${j.init}</div><div class="judge-name">${j.name}</div><div class="judge-role">${j.role}, ${j.company}</div><div class="judge-tags">${j.tags.map(t=>`<span class="judge-tag">${t}</span>`).join("")}</div>`;grid?.appendChild(d);});
 const list=document.getElementById("faq-list"); faq.forEach(([q,a],i)=>{const d=document.createElement("div");d.className="faq-item reveal";d.innerHTML=`<button class="faq-q" aria-expanded="false" aria-controls="faq-a-${i}">${q}<span class="faq-toggle" aria-hidden="true">+</span></button><div class="faq-a" id="faq-a-${i}" role="region"><div class="faq-a-inner">${a}</div></div>`;list?.appendChild(d); const b=d.querySelector("button"); b.addEventListener("click",()=>{const open=d.classList.contains("open");document.querySelectorAll(".faq-item.open").forEach(x=>{x.classList.remove("open");x.querySelector("button")?.setAttribute("aria-expanded","false")});if(!open){d.classList.add("open");b.setAttribute("aria-expanded","true")}});});
 document.querySelectorAll("a.hero-cta,.nav-cta").forEach(a=>{if(a.tagName==="A")a.href=REGISTRATION_URL;});
}
